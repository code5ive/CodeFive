namespace CodeFive
{
    partial class Form1
    {
        // This holds all the controls and components for the form.
        private System.ComponentModel.IContainer components = null;

        protected override void Dispose(bool disposing)
        {
            if (disposing && (components != null))
            {
                components.Dispose();
            }
            base.Dispose(disposing); // Always call the base dispose as well
        }

        #region Windows Form Designer generated code

        // It sets up all the controls on the form
        private void InitializeComponent()
        {
            
        }

        #endregion

        
    }
}
